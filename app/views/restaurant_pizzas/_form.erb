<h1>New Pizza Form<h1>

<%= form_for @restaurantpizza do |f|%>
<%= f.label :name %>
<%= f.text_field :name %>
<br>
<%= f.label :ingredients %>
<%= f.text_field :ingredients %>

<%= f. collection_select :restauran_id, Restaurant.all, :id, :name%>
<%= f.submit%>
